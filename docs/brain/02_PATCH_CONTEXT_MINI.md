# Patch Context Mini

## Current architecture

- `app.py` should stay thin and should not be edited unless explicitly requested.
- UI files should only be edited in UI-specific patches.
- API routes should only be edited in API-specific patches.
- `services/` contains pure Python engines and business logic.
- `tests/` contains deterministic local tests.
- `docs/brain/` contains project context and terminal commands.
- Supabase SQL lives in `supabase/`.
- Discord logic is split into formatter, sender, and filter services.
- Signal flow: heatmap history -> wall events -> persistence features -> setup classifier -> signal builder -> signal journal -> alerts.
- Whale flow starts with whale event detection, then formatter/filter/sender later.
- Whale live pipeline: Binance aggTrade WS -> normalize -> detect_whale_events -> filter -> (Discord | JSONL journal | Supabase whale_events).
- Whale read path: website /api/whale-alerts reads Supabase -> local JSONL journal -> mock alerts (server-side, 3-tier fallback).
- Futures/leverage context (LM64 series, planned): Binance futures aggTrade + funding/OI poller + force-order stream feed `MarketContext` (funding_rate, oi_change_pct, derived leverage_heat) into existing whale events without claiming individual-account leverage. See `docs/brain/LM64_FUTURES_WHALE_SOURCE_DISCOVERY.md`.
- Landing page redesign (LM67 series, planned): signature hero "The Lumora Field" — a 2.5D SVG/CSS market pressure visualization (price path, liquidity bands, whale pulses, futures drift, sweep zones, Current Read badge) replacing the generic hero; new sections on Panel/lm-* design language. See `docs/brain/LM67_LANDING_PAGE_DISCOVERY.md`.
- Global visual cleanup (LM69 series, planned): art-directed app-wide cleanup — one instrument per page, 3-level Surface system (border is earned), StatusChip semantic re-token (demo = gray, amber = risk/stale only), 5 typography roles, telemetry behind "System" Disclosures, debug UI out of product. Plan in `docs/brain/LM69_GLOBAL_VISUAL_CLEANUP_DISCOVERY.md`. Sequence: LM69B shell+primitives, LM69C terminal, LM69D dashboard/IA, LM69E status/typography standardization.
- Unified Intelligence Chart (LM68 series): lightweight-charts candlestick base with optional Lumora overlays (liquidity zones, whale markers, futures context, current read, sweep risk) behind three view modes Clean/Assisted/Full Intel; layered architecture + data contracts in `docs/brain/LM68_UNIFIED_INTELLIGENCE_CHART_DISCOVERY.md`. LM68B (mock panel) and LM68C (live Binance candles via public REST klines + kline WS in `lumora-web/lib/binance-klines.ts` + `useBinanceKlines`, with REST-poll and demo fallbacks) are done; LM68D whale markers are live (client hook `useWhaleChartEvents` polls /api/whale-alerts 30s, `lib/chart-whale-events.ts` maps real events to candle-snapped markers; demo markers remain the fallback); heatmap zones / read overlays still demo.

- Gold Bot (LM73/LM74 series): private XAUUSD daytrading assistant — paper/demo first, no live trading. UI command room lives at `lumora-web/app/(app)/gold-bot/` (status strip with Watch/Hunt/Review + risk modes Safe/Balanced/Aggressive, staged XAUUSD chart instrument, bot-brain rail, engine feed, planned modules for daily limits/funded guardrails/execution targets/Discord review). Engine architecture (sources → memory → replay → strategies → risk → manager → execution → journal → review) is specified in `docs/brain/LM74_GOLD_BOT_CORE_ENGINE_BLUEPRINT.md`; planned code home `services/goldbot/`, MT5 demo as first real execution path behind safety flags (MT5_DEMO_ONLY, LIVE_TRADING_ENABLED=false, ALLOW_REAL_ORDERS=false, KILL_SWITCH). Risk Engine has final authority: +10%/day aspirational only, -7%/day hard stop, no martingale/revenge, news lockout; self-learning = journal + analytics + owner-approved suggestions only.

## Completed patches

- LM45 heatmap history
- LM46 wall events
- LM47 wall persistence features
- LM48 setup classifier
- LM49 signal builder
- LM50 signal journal
- LM51A discord formatter
- LM51B discord webhook sender
- LM51C discord alert filter
- LM52A whale alert engine
- LM63A whale source discovery (recommendation only)
- LM63B Binance aggTrade collector
- LM63C whale live smoke + Discord pipeline
- LM63D per-symbol whale thresholds
- LM63E local JSONL whale event journal
- LM63F whale feed API + website fallback to mock
- LM63G Supabase whale_events schema (+ unique constraint fix)
- LM63H Supabase writer + smoke --target wiring
- LM63I website Supabase tier (Supabase -> journal -> mock)
- LM63J whale worker mode (--forever, --heartbeat-interval, env config)
- LM63K whale worker deployment docs
- LM64A futures whale source discovery (recommendation only)
- LM64B Binance Futures aggTrade connector (--market spot|futures; no schema change)
- LM67A landing page discovery — The Lumora Field (plan only, no frontend code)
- LM67B–LM67F landing page implementation — Lumora Field hero, signal tape, feature grid, cinematic polish, dynamic headline (framer-motion)
- LM68A unified intelligence chart discovery (plan only, no frontend code)
- LM68B intelligence chart mock panel (lightweight-charts, Clean/Assisted/Full Intel modes, overlay toggles, mock scene; mounted on Terminal + Liquidity Map)
- LM68C Binance kline live candles (public REST snapshot + kline WS, REST poll fallback, demo fallback on failure; overlays still demo, derived relative to displayed candle range)
- LM69A global visual cleanup discovery (plan only, no frontend code)
- LM69B app shell + panel primitives (PageShell, MetricStrip, Panel levels default/focus/subtle, StatusBadge re-token with gray `demo` variant, flat app bg, TopNav UTC clock; all five app pages adopted)
- LM69C UI texture + interaction pass (Panel depth: inner highlight + soft drop on default, instrument shadow + built-in cyan top stripe on focus; StatusBadge faint matching borders; PageShell header hairline; TopNav LIVE chip + active icon tint; chart header group dividers, aria-pressed segments, overlay toggles with cyan indicator dots, bias-railed read chip, two-sided honesty footer; Dashboard's oversized Current Read card replaced by a one-line command strip — bias/score/conf/risk/action/price/status — with watchlist + whale tape in the right rail)
- LM69C dock + visual liveness pass (`components/ui/IntelligenceDock.tsx`: dark glass pill with icon controls — hover halo + 1px lift + compact tooltip + active dot + optional live badge, tones cyan/violet/emerald/rose/amber, CSS transitions with motion-reduce gating; chart header mode presets Clean/Assisted/Full Intel (violet) + overlay channels Heatmap/Whales/Pressure/Read (semantic tones) now live in the dock; chart pane gained faint cyan/violet edge-light hairlines and an emerald halo on the LIVE badge; Terminal lost the perpetual walls spinner and gained a cyan-edged pressure caption)
- LM69C nav refinement fix (TopNav rebuilt as a dark-glass command bar styled fully in-component — violet-tiled logo mark + LUMORA + TERMINAL suffix, active link = soft violet inset pill with luminous violet→cyan bottom edge, hover = 1px lift + inner highlight (motion-reduce gated), UTC clock + LIVE dot fused into one bordered status capsule, faint violet edge-light hairline along the nav bottom; the old global .lm-topnav/.lm-nav-link classes are unused by TopNav. Chart fix: the futures pressure strip is now an absolute bottom overlay INSIDE the chart pane — toggling Pressure/Read no longer changes the instrument height, nothing renders below the chart)
- LM68D real whale markers on chart (client `useWhaleChartEvents` polls /api/whale-alerts; `lib/chart-whale-events.ts` filters by symbol, snaps event_ts to candle buckets, dedupes per bucket+side, caps Assisted 6 / Full 12 strongest; BUY below / SELL above bar, HIGH risk = bigger marker, short $-labels; loader exposes raw event_ts/notional_usd/severity/source_type on WhaleAlertView; footer + dock hint show whales live/fallback/none; demo markers stay as fallback)
- LM69C final nav/dashboard/tooltip fix (dock tooltips now float ABOVE each control — minimal-dock language, fade+rise on hover, hidden under md; IntelligenceChartPanel dropped its panel-level overflow-hidden so upward tooltips aren't clipped (the chart pane clips itself, footer rounds its own corners); TopNav pushed further: navy gradient glass, links ride inside ONE recessed channel rail, active = violet gradient pill + violet→cyan luminous edge, hover halo + press scale, gradient brand tile with soft glow, UTC/LIVE in a matching recessed rail; app backdrop is deep navy `#0a0b10` with a faint indigo top wash (layout.tsx); Dashboard rescue: read strip gained a bias-colored left edge + faint cyan→violet interior wash + glowing live price, setups rows carry bias side-rails with bias-toned score bars, watchlist confidence bars are bias-toned, subtle panels get a faint white ring for finish)

- LM73A gold bot command room page (`/gold-bot` route + Gold Bot nav tab with amber icon accent; paper-mode page with status header, bot brain, zone detection, risk engine, paper trading, learning journal — all staged data)
- LM73C gold bot visual upgrade (three-column command room: center staged XAUUSD SVG chart instrument with sweep+reclaim narrative/FVG/key levels/watching ring, left bot-brain rail with analytical meters + detector stack, right engine feed + risk permission + paper state; gold ambient field, scanner sweeps, Watch/Hunt/Review mode tabs, command-room footer)
- LM73D gold bot intelligence content pass (risk modes Safe/Balanced/Aggressive in status strip, TARGET +10% GOAL / MAX LOSS -7% HARD STOP chips, trade-permission evaluation checklist with STAY FLAT verdict, macro/spread meters, planned-modules band: daily limits / funded guardrails FTMO·The5ers·Alpha·Custom / execution targets with live adapter LOCKED / Discord review; feed types STRATEGY/NEWS/LEARNING/DISCORD/EXEC; 9-field journal contract with owner-approval rule)
- LM74A gold bot core engine blueprint (plan only, no code: `docs/brain/LM74_GOLD_BOT_CORE_ENGINE_BLUEPRINT.md` — 10-layer architecture, frozen-dataclass pipeline MarketContext→StrategySignal→RiskDecision→TradeIdea→OrderResult→ExitRecord→JournalEntry→ReviewReport, 7 strategy families, MT5 demo execution behind safety flags, historical backfill + no-lookahead replay learning, implementation sequence LM74B–LM75x)
- LM75D MT5 demo risk/margin/lot calculator (DEMO ONLY: pure `services/gold_bot_lot_calculator.py` (risk modes safe 0.25/balanced 0.50/aggressive 1.0/experimental 0.10%, caps 1.0% default / 2.0% with --allow-high-demo-risk, `calc_auto_volume` scales largest lot whose order_calc_profit SL loss ≤ target, rounds down to volume_step, steps down on broker non-linearity, fails closed). Connector gains `account_snapshot`/`symbol_metadata`/`compute_levels`/`calc_margin`/`calc_profit`/`estimate_sl_loss` (order_calc_margin/profit). Risk gate `evaluate_risk_plan` = `_core_safety` (no fixed-lot cap) + daily-loss budget (equity×7% − today realized loss, blocks if est SL loss > remaining or stop reached) + margin ≤10% free margin + trade-count from today XAUUSD entry deals. Trade loop adds `--auto-volume`/`--risk-mode`/`--risk-pct`/`--allow-high-demo-risk`, prints full risk panel, journals risk fields. Manual --volume still works + shows est SL loss/risk%. Sending still needs --confirm-demo-order on verified demo. Live-verified: balanced auto→1.92 lot/497.6 risk APPROVED, aggressive→3.86 lot BLOCKED on margin, no-confirm/dry-run send nothing. LM75D-followup: removed the hardcoded 3-trades/day cap — `SafetyConfig.max_trades_per_day` defaults None = high-activity (no cap), enforced only when `--max-trades-per-day N` or env `GOLD_BOT_MAX_TRADES_PER_DAY` is explicitly set; panel shows "Frequency mode: high-activity / Trades today: X / unlimited" or the configured cap. All other risk controls unchanged. Plus `scalp` risk mode (0.10% risk/trade, demo-only high-frequency M1/M5; prints "Frequency mode: experimental-scalp"; small-cap like experimental even with override; obeys demo/SL-TP/daily-stop/margin/kill-switch, no martingale, no live). Live-verified: scalp→0.96 lot/99.5 risk APPROVED, nothing sent)
- LM75C-fix MT5 deal/history import (READ-ONLY: previous `read_history` queried one tz-aware-UTC 7d window with `date_to=now`, so a broker server clock ahead of UTC excluded just-closed deals and never read `history_orders_get` — probe showed "0 deals" despite a -2.58 balance change. New `deal_history`/`order_history`/`summarize_deals`/`deal_view` + `read_history_report` query today/7d/30d, pad `date_to` +1 day, count deals+orders+XAUUSD+entry/exit and sum profit/commission/swap on trade deals only; `--history-debug` prints recent deal rows + exact windows; empty result prints likely reasons. Live-verified: the LM75B demo BUY + stop-out SELL now appear, profit -2.58 matching the balance. Fixed a cp1252 crash from a `→` char in probe output)
- LM76B Gold Bot strategy engine V2 (DEMO ONLY, decision-only default: upgraded `services/gold_bot_decision_engine.py`. MarketState gains swing high/low, prev high/low (sweep candidates), wick ratios, compression (compressed/expanding/normal), regime (trend/range), session (Asia/London/NewYork/Off-session from UTC). Detectors run as candidates, best chosen by priority then confidence: liquidity_sweep_reclaim (sweep prior level + reclaim/rejection wick) > fvg_retest (3-candle imbalance + retest + momentum confirm) > breakout_retest (range break + retest near level) > momentum (SMA stack) > scalp_retest/scalp_momentum (scalp mode, fast momentum + micro pullback, SL/TP 120/180). Explainable `confidence_components` (base + setup + sweep/reclaim/fvg/retest + penalties spread/vol/extension + session mod). NO_TRADE strategies no_trade_spread/no_trade_chop/manage_existing/no_trade with watch reasons + detected zones. Chaotic filter refined: outsized range only blocks when no dominant rejection wick (clean sweeps pass). Probe prints session/regime/structure/zones/components. Tests rewritten (sweep LONG/SHORT, FVG detect, breakout, chop/spread/chaotic/open-position NO_TRADE, scalp SL/TP, V2 context). Live-verified: real M1 → SHORT fvg_retest conf 81 / scalp_momentum conf 80, nothing sent. Same guards as V1)
- LM76A Gold Bot decision engine V1 (DEMO ONLY, decision-only by default: pure `services/gold_bot_decision_engine.py` reads recent XAUUSD candles → `MarketState` (SMA9/21, recent high/low, body strength, momentum pts, volatility, spread, dist-from-extreme) → `TradeIdea` LONG/SHORT/NO_TRADE with reasons/blockers/confidence/SL-TP. Detectors: momentum continuation (SMA stack + momentum), breakout_watch (near recent extreme), scalp_momentum (faster, SL/TP 120/180, tighter spread ceiling), no_trade (insufficient/wide-spread/chaotic/open-position/low-confidence). `scripts/run_gold_bot_decision_probe.py` reads MT5 via `connector.recent_candles`, prints decision + reasons, journals to `data/gold_bot/decision_journal.jsonl` (gitignored). Execution only for LONG/SHORT through LM75D risk plan AND both --auto-execute-demo + --confirm-demo-order on demo with no open XAU position; default sends nothing. Position stacking disabled V1 (open XAU → manage_existing NO_TRADE). Tests `tests/test_gold_bot_decision_engine.py` (bullish→LONG, bearish→SHORT, high-spread/chaotic/open-position/insufficient→NO_TRADE, scalp SL/TP). Live-verified: real M1 trend→SHORT conf 86, NO_TRADE breakout_watch when mixed, nothing sent)
- LM75E MT5 demo close dry-run + richer post-close (DEMO ONLY: `--close-position TICKET --dry-run` now previews the close request (build + "would close ... via SELL/BUY @ price") and sends nothing; confirmed close prints post-close balance/equity + today realized PnL via LM75C history; list shows XAUUSD open count; `--emergency-close-demo` alias of `--emergency-close`; journal records dry_run/blocked/close_position + profit_before. Live-verified: list 0 (XAU 0), bogus-ticket dry-run/confirm both "not found" no send; build_close_request/send_demo_close covered by unit tests. Open→dry-run-close→confirm-close cycle is a manual user step)
- LM75C MT5 demo position manager + close (DEMO ONLY: `--list-positions` (read-only) and `--close-position TICKET` added to `scripts/run_mt5_demo_trade_loop.py`; connector gains `all_positions`/`position_by_ticket`/`position_view`/`build_close_request`/`send_demo_close` (last-line demo_verified guard); pure `evaluate_demo_close` in risk gate. Close sends nothing unless `--confirm-demo-order` + verified demo + ticket found; closing is risk-reducing so open-side caps don't apply, but kill switch still blocks unless `--emergency-close` (demo only). Close BUY→SELL@bid, SELL→BUY@ask using exact ticket/symbol/volume; order_check on close is advisory only. Post-close re-checks the ticket + remaining symbol positions; journals mode close_position/blocked. Live-verified on MetaQuotes-Demo: list + bogus-ticket close both blocked with nothing sent)
- LM75B MT5 demo guarded trade loop (DEMO ONLY, default sends nothing: `scripts/run_mt5_demo_trade_loop.py` + demo order helpers in `mt5_demo_connector.py` + pure `services/gold_bot_risk_gate.py` + `services/gold_bot_trade_journal.py`. Order sent ONLY with `--confirm-demo-order` (not `--dry-run`), on a strict-demo account (connector `demo_verified`, last-line guard in `send_demo_order`), after the risk gate approves (demo/side/volume≤max/SL+TP required/kill-switch/LIVE+REAL flags/daily-trade-cap from journal) AND order_check passes. SL/TP mandatory + orientation-validated (buy SL<price<TP, sell TP<price<SL). Safety env defaults safe: MT5_DEMO_ONLY/LIVE_TRADING_ENABLED=false/ALLOW_REAL_ORDERS=false/GOLD_BOT_KILL_SWITCH=false/MAX_TRADES_PER_DAY=3/MAX_DAILY_LOSS_PCT=7 (daily-loss math is TODO warning, not yet computed). Journal `data/gold_bot/demo_trade_journal.jsonl` (gitignored) records every attempt dry_run/blocked/demo_order. Close-position deferred. Tests `tests/test_gold_bot_risk_gate.py` + extended connector tests, fake MT5, no terminal needed)
- LM75A MT5 demo connector probe (READ-ONLY, no orders: `services/connectors/mt5_demo_connector.py` + `scripts/run_mt5_demo_connector_probe.py`. Lazy/injectable MetaTrader5 module; fails closed if init fails, account_info None, account is real (trade_mode=2), no gold symbol selectable, or zero candles. Reads account/trade_mode/balance, discovers gold symbol via candidate list then XAU/GOLD scan, reads candles + tick/spread + positions + history; optional reads warn and continue. Safety flags MT5_DEMO_ONLY/LIVE_TRADING_ENABLED=false/ALLOW_REAL_ORDERS=false asserted in run_probe. No order_send/order_check anywhere. MetaTrader5 NOT added to requirements.txt — Windows-only, install via `pip install MetaTrader5`. Tests in `tests/connectors/test_mt5_demo_connector.py` with a fake MT5, no terminal needed)
- LM81A Gold Bot worker loop (DEMO ONLY, observe/dry-run default, SENDS NOTHING: new `services/gold_bot_worker.py` (`WorkerConfig` + `GoldBotWorker`) + thin `scripts/run_gold_bot_worker.py`. Long-running loop: each iteration reads MT5 demo account/symbol/tick/candles, builds LM77A macro context, runs Decision Engine V2, prints a safety banner once + a compact `[HB]` heartbeat per iteration, journals to `data/gold_bot/worker_journal.jsonl` (gitignored), writes `data/gold_bot/worker_status.json` (gitignored). CLI: `--mode observe|demo`, `--risk-mode`, `--interval-seconds`, `--max-iterations`, `--symbol`, `--timeframe`, `--bars`, `--macro-events-file`, `--dxy/--yields-bias`, `--geopolitical-risk`, `--auto-execute-demo`, `--confirm-demo-order`, `--close-on-no-trade` (PLACEHOLDER, never closes in V1), `--no-status-file`, `--json`. Safety: observe never sends; demo sends ONLY with --auto-execute-demo + --confirm-demo-order, on verified demo, after LM75D risk gate APPROVES; macro lockout -> NO_TRADE (engine); open XAUUSD -> no stacking; kill switch / LIVE / REAL flags block (risk gate); non-demo account / live flags / MT5 unavailable for 5 consecutive iters -> CriticalSafetyError stop (exit 2). One bad iteration logs + continues. Reuses the LM76/LM77 probe sizing+risk path (magic 810810). Tests `tests/test_gold_bot_worker.py` (11, fake connector, no terminal): observe/demo gating, kill-switch block, lockout block, no-stack, non-demo fail-closed, journaling/status, max-iterations. Decision probe + MT5 trade loop unchanged)
- LM82A Gold Bot start scripts (PowerShell convenience launchers, no core/strategy/UI change: `scripts/start_gold_bot_observe.ps1` balanced observe, `start_gold_bot_scalp.ps1` scalp observe, `start_gold_bot_macro_scalp.ps1` scalp observe + sample macro lockout context, `start_gold_bot_demo_guarded.ps1` (DEFAULTS observe/dry-run; only passes `--auto-execute-demo --confirm-demo-order` when run with `-ConfirmDemoExecution`; optional `-RiskMode`/`-IntervalSeconds`), `gold_bot_health_check.ps1` one-shot read-only connector probe + decision probe dry-run. All MT5 demo only, never live, no secrets; resolve repo root via `$PSScriptRoot` (work from repo root or scripts folder); `$ErrorActionPreference=Stop` (errors not hidden). Observe/scalp scripts send nothing; generated worker journal/status stay gitignored)
- LM83A Economic calendar source layer (provider-neutral, no paid API / secrets / scraping: new `services/gold_bot_economic_calendar.py` normalizes raw entries into `EconomicEvent` (event_id/title/event_type/currency/impact/scheduled_at UTC/country/previous/forecast/actual/source/source_url/raw_payload/tags). Type inference (NFP/CPI/FOMC/Fed Speech/Jobless Claims/Retail Sales/PMI/GDP/Interest Rate Decision/Treasury Auction/Other); USD high-impact CPI/NFP/FOMC/Fed Speech/Rate Decision tagged `gold_major`. Providers: `LocalJsonEconomicCalendarProvider` (REAL, reads `data/gold_bot/economic_calendar.sample.json`), `FutureApiEconomicCalendarProvider` + `ManualImportProvider` (placeholders, no network/key). Loader normalizes+sorts, skips malformed with warnings, missing file -> ([],"none",[warn]); `filter_events` (currency/impact/forward window, keeps recent past 30m), `next_high_impact`. New `scripts/run_gold_bot_calendar_probe.py` (read-only). `gold_bot_macro_context.load_calendar_or_macro(calendar_file|macro_events_file)` prefers calendar, source_mode `economic_calendar:local_json`, falls back to LM77A macro events; lockout/watch/post-event rules unchanged. Decision probe + worker gain `--calendar-file` (preferred) keeping `--macro-events-file` as fallback. Tests `tests/test_gold_bot_economic_calendar.py` (18: parse/normalize/type-infer/gold_major/malformed-skip/sort/filter/window/next-high-impact/state lockout-clear-watch/missing-file/providers/macro integration) + macro tests. Existing decision probe + worker still run without any calendar file)
- LM83A part2 Data source architecture (provider-neutral, NO HTTP/keys/secrets in this patch: new `services/gold_bot_data_sources.py` with `ProviderStatus` model (name/category[economic_calendar|historical_market|macro_market|news|manual]/status[active|fallback|placeholder|missing|error]/freshness/last_updated/message/warnings) + registry `build_data_source_overview`/`group_by_category`. REAL: `LocalJsonEconomicCalendarProvider` (status active) + `ManualJsonEconomicCalendarProvider` (status fallback, `data/gold_bot/economic_calendar.manual.json` = TEMPORARY free fallback, emits reminder warning). PLACEHOLDER classes (status-only, inert fetch, no network): Finnhub/TradingEconomics/MT5CalendarExport (calendar), GDELT/RSS (news), FRED/yfinance (macro), MT5HistoricalXauusd/Dxy/UsYields/Vix history (historical, until LM84A/LM84B). `resolve_calendar_provider(path)` -> Manual if filename has `manual` else Local; `.status(now)` reports active/fallback/missing/error + fresh/stale/none from file mtime+events. New `scripts/run_gold_bot_data_sources_probe.py` prints grouped overview (no HTTP). Calendar probe + decision probe + worker now show provider status; `--calendar-file` accepts sample or manual. Tests `tests/test_gold_bot_data_sources.py` (status model, overview active/fallback/missing, placeholders inert) + manual-provider/status/resolve tests. `.gitignore` also tracks `*.manual.json`. No external HTTP anywhere; existing probe/worker still run with no calendar file)
- LM84A Historical market backfill (REAL MT5 XAUUSD history, READ-ONLY, no orders/HTTP: new `services/gold_bot_historical_market_data.py` - `HistoricalBar` model (symbol/timeframe/time UTC/OHLC/tick_volume/spread/real_volume/source/loaded_at), CSV write/read, metadata build/read (rows_written/first+last bar/requested range/generated_at/warnings), `quality_summary` (duplicates/missing-or-zero OHLC/high<low/non-monotonic/time-gaps/spread min-avg-max), `MT5HistoricalXauusdProvider.backfill` (verifies DEMO acct, fetches per timeframe M1/M5/M15/H1 via connector's new read-only `copy_rates_range`, normalizes UTC, writes `data/gold_bot/history/XAUUSD_<TF>.csv` + `.meta.json`, skips existing unless --overwrite, dry-run writes nothing, warns when MT5 returns < ~half expected bars, never crashes), `history_status`/`list_history` -> ProviderStatus active/missing + fresh/stale from file metadata. Connector gains read-only `copy_rates_range` (tick_volume/spread/real_volume, may return empty). New scripts `run_gold_bot_history_backfill.py` (`--symbol/--timeframes/--days/--from-date/--to-date/--out-dir/--overwrite/--dry-run`) + `run_gold_bot_history_probe.py` (`--history-dir/--symbol/--timeframe/--tail/--json`). data_sources: MT5 XAUUSD history is now REAL (active/missing) not placeholder; DXY/yields/VIX stay placeholder until LM84B; `build_data_source_overview` gains `history_dir`. Tests `tests/test_gold_bot_historical_market_data.py` (CSV roundtrip, metadata, quality detects dupes/bad-OHLC/gaps, backfill via injected fetch_fn, dry-run no-write, no-overwrite skip, empty-range warn, status missing/active/fresh/stale) - no MT5. Generated `data/gold_bot/history/*.csv|*.json` gitignored)
- LM84B Macro history import (DXY/US10Y/US02Y/VIX offline CSV bootstrap, NO HTTP/API/keys/scraping: new `services/gold_bot_macro_history.py` - `MacroBar` model (symbol/timeframe/time UTC/open-high-low opt/close/value opt/source/loaded_at), `load_macro_csv` auto-detects OHLC (`time,open,high,low,close`) or value-only (`time,value`; close=value, OHLC None), skips malformed rows with warnings; `import_csv` validates+normalizes -> `data/gold_bot/macro_history/<SYM>_<TF>.csv` + `.meta.json` (dry-run writes nothing, skips existing unless --overwrite, input_missing/invalid handled); `quality_summary` (dup/missing-close/high<low/non-monotonic/gaps); `macro_market_statuses` -> ProviderStatus dxy_history/us_yields_history(US10Y|US02Y)/vix_history active|missing + fresh/stale (category macro_market). data_sources: DXY/yields/VIX now REAL (active/missing) not placeholder; FRED+yfinance stay placeholder; `build_data_source_overview` gains `macro_history_dir`. New scripts `run_gold_bot_macro_history_import.py` (`--input/--symbol/--timeframe/--source/--out-dir/--overwrite/--dry-run`) + `run_gold_bot_macro_history_probe.py` (`--history-dir/--symbol/--timeframe/--tail/--json`). Tracked: `data/gold_bot/macro_history/README.md` + `samples/*.sample.csv` (DXY/VIX OHLC, US10Y/US02Y value); ignored: `macro_history/*.csv|*.json`. Tests `tests/test_gold_bot_macro_history.py` (OHLC/value parse, malformed skip, metadata, dry-run no-write, overwrite protect, quality detects dup/high<low/non-monotonic, status missing/active per instrument) - no internet/MT5. LM84A MT5 history + existing decision/worker/calendar unaffected)
- LM85A No-lookahead replay engine (backtest, OFFLINE, never MT5/orders/HTTP: new `services/gold_bot_replay_engine.py`. `ReplayClock` streams stored XAUUSD history (LM84A `read_bars_csv`) chronologically yielding `ReplayStep` with `visible_bars` (time <= current, bounded window, NEVER future); warmup_bars/max_bars/from_time/to_time. `MacroSeries.load`+`snapshot` join LM84B DXY/US10Y/US02Y/VIX D1 AS-OF current time (bisect, latest row <= t, never future) -> dxy/us10y/us02y/vix close + dxy_bias/yields_bias/vix_bias/risk_bias; missing macro -> unknown + warnings, no crash. `replay_decide` adapter converts visible bars -> candle dicts, spread from current bar, has_open_position=False, builds MacroContext from as-of biases, runs real `decide()` (no MT5, execution impossible). Forward scoring (`score_horizon`/`score_forward`) reads `bars[i+1:]` ONLY AFTER the decision is recorded: per horizon ret_points/dir_return/MFE/MAE/TP-SL first-touch/outcome win|loss|neutral|no_data; NO_TRADE -> outcome no_trade (not win/loss), price move still recorded. `run_replay` writes `data/gold_bot/replay/replay_XAUUSD_<TF>_<date>_NNN.jsonl` (per-step rows incl `no_lookahead_visible_bars_count` + `forward_scoring_uses_future_after_decision:true`) + `.summary.json` (bars/long/short/no_trade/avg conf/by-horizon win-loss-neutral-no_data+avg return/top setups/warnings); dry-run writes nothing; missing history -> ReplayError with LM84A hint. New `scripts/run_gold_bot_replay.py` (`--symbol/--timeframe/--history-dir/--macro-history-dir/--out-dir/--warmup-bars/--max-bars/--from-time/--to-time/--risk-mode/--horizons/--dry-run/--json`). Tests `tests/test_gold_bot_replay_engine.py` (16: chronological iter, visible never future, from/to+warmup+max, macro as-of excludes future row, missing macro unknown, LONG/SHORT/NO_TRADE/no_data scoring, TP-hit win, dry-run no files, summary counts, MetaTrader5 never imported) - no MT5/internet. Generated `data/gold_bot/replay/*` gitignored)
- LM86A Pattern scoring / learning journal (replay -> setup scorecards, OFFLINE/READ-ONLY, no MT5/orders/HTTP, NO live wiring: new `services/gold_bot_learning_journal.py`. `load_replay_rows` reads `data/gold_bot/replay/*.jsonl`, enriches each row with risk_mode (from sibling `.summary.json`) + session (derived from bar time when absent), tolerates missing/bad fields with warnings. `build_scorecard` aggregates global + per-setup: sample/trade/no_trade/long/short, avg_confidence, win/loss/neutral/no_data + winrate + avg_dir_return + expectancy_points BY HORIZON, avg MFE/MAE; slices by confidence bucket (0-39..80-100), direction (LONG/SHORT), timeframe, risk_mode, session, macro (dxy_bias/yields_bias/risk_bias/vix_bias, unknown fallback); NO_TRADE missed-opportunity (count/avg raw move/large moves > threshold). `recommended_status` = insufficient_sample(trade_count<min) / avoid(exp<0 & winrate<0.45) / promising(exp>20 & winrate>=0.50) / weak(|exp|<=20 or 0.45<=winrate<0.50) / mixed; default --min-samples 20, --missed-move-threshold-points 150. `build_setup_modifiers_preview` -> `setup_modifiers.preview.json` (promising +5/weak -3/avoid -8) but PREVIEW ONLY: `LEARNING_MODIFIERS_LIVE=False`, NOT imported by decision engine/worker (test asserts). New `scripts/run_gold_bot_learning_scorecard.py` (`--replay-dir/--out-dir/--symbol/--timeframe/--risk-mode/--horizon/--min-samples/--missed-move-threshold-points/--top/--dry-run/--json`): dry-run writes nothing, no replay files -> exit 1 with hint. Writes scorecard_<sym>_<tf>_<risk>_<date>.json + scorecard_latest.json + preview + learning_events.jsonl (all gitignored `data/gold_bot/learning/*`). Tests `tests/test_gold_bot_learning_journal.py` (load/enrich, missing fields, status rules, setup/confidence/direction/tf/risk/macro slices, no_trade missed, min-sample guard, preview-not-live, dry-run no files, decision engine doesn't import learning). LIVE DECISION/WORKER BEHAVIOR UNCHANGED)
- LM86C Automatic learning cycle (replay->learn->adapt, DEMO-ONLY, NO live/orders/MT5/HTTP, confidence-only: new `services/gold_bot_learning_cycle.py` chains baseline replay (learning OFF) -> LM86A scorecard/preview -> promote CANDIDATE (`candidate_demo_modifiers.json`, NEVER touches active) -> candidate replay (learning ON, explicit candidate file) -> objective compare at the selected horizon -> keep or rollback. `extract_summary` reduces an LM85A summary to {trade_count(win+loss+neutral+no_data), winrate(win/(win+loss)), expectancy=avg_trade_return_points, no_trade_count, decisions, top_setups, no_data flag}. `compare_summaries` PRIMARY=expectancy improvement; accept iff candidate_exp >= baseline_exp + min_improvement_points(10) AND candidate_trades >= min_trades(30) AND candidate_trades >= baseline_trades*min_trade_count_ratio(0.5) AND winrate not down > 0.05 unless expectancy improved strongly (>= 2x floor); baseline/candidate no_data -> reject safely. Accept -> `activate_candidate` backs up old active to `active_demo_modifiers.backup.json` then writes candidate as active tagged decision=accepted; reject -> `write_rejected` -> `rejected_demo_modifiers.json`, active untouched, no backup. `run_rollback`/`rollback_active` restore backup->active + append rollback event. Metadata on active/candidate: generated_at, expires_at (default now+7d), cycle_id, source_scorecard, baseline_summary, candidate_summary, accept/reject reason; `modifiers_expired` only WARNS (probe), no auto-delete. New `services/gold_bot_learning_modifiers.py` primitives: CANDIDATE_FILE/BACKUP_FILE/REJECTED_FILE, `_expires_at`, `build_modifier_set`, `promote_candidate`, `activate_candidate`, `write_rejected`, `rollback_active`, `modifiers_expired` (existing promote/load untouched). New CLI `scripts/run_gold_bot_learning_cycle.py` (`--symbol/--timeframe/--risk-mode/--max-bars/--horizons/--horizon/--min-samples/--min-trades/--min-improvement-points/--min-trade-count-ratio/--learning-dir/--replay-out-dir/--dry-run/--rollback/--json`): dry-run prints planned steps + validates history, writes nothing; heavy replay/scorecard steps injectable so tests run offline with fake summaries. Probe gains an expiry warning. Cycle summary `cycles/cycle_<ts>.json` + `cycles/cycle_events.jsonl` + candidate/backup/rejected all gitignored. Tests `tests/test_gold_bot_learning_cycle.py` (compare accept/reject worse/reject collapse/missing-safe/min-improvement, candidate not overwriting active, accepted backup+replace, rejected keeps active, rollback, expiry metadata, dry-run no-write, no MT5/orders/http in source, CLI defaults). Live-verified: real M1 balanced cycle baseline exp -41.1pt / candidate exp -50.7pt -> REJECTED (-9.6pt < +10pt floor), active kept, no backup; injected better candidate -> ACCEPTED, old active backed up, expires_at now+7d. Live trading impossible via the cycle)
- LM86B Demo auto learning modifiers (preview->active, DEMO-ONLY autonomy, NO live/orders/HTTP, confidence-only: new `services/gold_bot_learning_modifiers.py` - `LearningModifier` model + `clamp_modifier` (promotion [-12,+8], hard [-20,+12]); `evaluate_preview` validates/clamps each LM86A preview entry -> status active|insufficient_sample|rejected (sample<min or missing reason zeroed); `promote` writes `active_demo_modifiers.json` (mode demo_auto_learning, safety demo_only, no per-setup approval) + appends `modifier_events.jsonl`; `load_active_modifiers` returns only active entries hard-clamped, missing/invalid -> ({},warn) continue without. LM86A `build_setup_modifiers_preview` enriched with setup/status/sample_count/expectancy/winrate/horizon/source_scorecard so promotion can validate. decision engine `decide()` gains `use_learning_modifiers`(default False)/`learning_modifiers`/`learning_modifiers_file`/`learning_mode`; `_apply_learning` adjusts a LONG/SHORT idea's confidence (clamp 0-100), appends explainable reason, sets `idea.learning` {original/modifier/final/source/mode}, may drop to NO_TRADE `learning_low_confidence`; NEVER revives a NO_TRADE/macro_lockout (applied AFTER macro, only on directional ideas) - macro lockout + risk gate + kill switch still win, volume untouched. Worker (`--use-learning-modifiers`/`--learning-modifiers-file`, banner lines, heartbeat learning line, journal `learning`) loads modifiers fail-soft, passes learning_mode observe|demo; demo execution STILL needs `--auto-execute-demo --confirm-demo-order` + demo acct + risk gate. Replay (`--use-learning-modifiers`/`--learning-modifiers-file`, summary `used_learning_modifiers`/count/source) - no MT5. New `scripts/run_gold_bot_learning_modifiers_probe.py` (`--learning-dir/--promote/--min-samples/--json`). Tests `tests/test_gold_bot_learning_modifiers.py` + decision/replay/worker additions (clamp, insufficient rejected, promote writes active+events, load missing/invalid/hard-clamp, reason surfaced, 0-100 clamp, macro lockout beats boost, learning off by default, replay/worker accept flags no MT5). Generated `data/gold_bot/learning/active_demo_modifiers.json`+`modifier_events.jsonl` gitignored. Default OFF everywhere; live trading impossible via learning)

## Rules

- No UI unless requested.
- No API route changes unless requested.
- No secrets.
- No commit/push.
- Tests must be local and deterministic.
- No network in tests.
- Keep patches small.
- Edit only listed files.

## Standard test commands

```bash
python -m pytest <target_test>
python -m compileall services tests